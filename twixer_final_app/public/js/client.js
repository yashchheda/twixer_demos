$(document).ready(function() {
    /*$("#result").html(renderjson.set_show_by_default(true)({
        outcome: '',
        wins: 0,
        losses: 0,
        ties: 0
    }));*/
    //console.log("Ready for jquery");

});
/*
$( "#twiiter_login" ).submit(function( event ) {
  //alert( "Handler for .submit() called." );
  event.preventDefault();
    var data = $("#twiiter_login").serialize();
    $.ajax({
        url: '/login',
        type: 'post',
        data:data,
        dataType: 'html',
        success: function(data) {
          //console.log(data);
          if(data==="error")
          {
              alert('Invalid Password');
          }
          else
          {
            //$("body").html(data);
            //$("#detail").append("<p><h3>User information</h3></p><p>Name: Dhaval</p>Bio: Here</p>");
          }

        }
    });
});

$( document ).on( "click","#logout", function( event ) {
  event.preventDefault();
  console.log("Logout Clicked");

});
*/
